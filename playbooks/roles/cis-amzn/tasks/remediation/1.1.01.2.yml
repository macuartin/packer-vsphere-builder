---

- name: Set check facts
  set_fact:
    cis_code: &cis_code '1.1.1.2'
    cis_name: &cis_name Ensure mounting of freevxfs filesystems is disabled
    cis_profile: &cis_profile level-1
    cis_scored: &cis_scored scored
    cis_section: &cis_section section-1
  tags:
    - always

- name: "CIS {{ cis_code }} - {{ cis_name }}"
  block:
  - import_tasks: ../commons/disable-filesystem.yml
    vars:
      filesystems_to_disable:
      - freevxfs
      cis_code: *cis_code
  tags:
    - *cis_profile
    - *cis_section
    - *cis_code
    - *cis_scored

...
